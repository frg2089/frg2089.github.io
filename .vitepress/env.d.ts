declare const __HOLIDAY__: Record<
  number,
  {
    year: number
    region: 'CN'
    dates: Array<{
      date: `${number}-${number}-${number}`
      name: string
      name_cn: string
      name_en: string
      type: string
    }>
  }
>
